// Only what differs per class. Everything else is in autoexec / the profile.
// Exists mainly to put mouse2 back after scout.cfg rebinds it.
bind mouse2 +attack2

// Per-slot viewmodels: guns hidden, melee and utility shown. Re-exec'd per class so
// another class's overrides (Spy's sapper) cannot leak in.
exec viewmodels
